[package]
name = "bcp-wire"
version = "0.1.0"
edition = "2024"
description = "Wire format types and serialization for the Bit Context Protocol"
license = "MIT"
repository = "https://github.com/mega-blastoise/bit-context-protocol"
homepage = "https://bitcontextprotocol.com"
keywords = ["bcp", "context", "wire-format", "binary"]
categories = ["encoding", "data-structures"]

[dependencies]
thiserror = { workspace = true }

[dev-dependencies]
